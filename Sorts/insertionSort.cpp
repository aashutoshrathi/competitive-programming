#include <bits/stdc++.h>
#include <stdio.h>

using namespace std;

void insert(int arr[], int i) {
    int key = arr[i];
    int j = i;
    
}

void insertionSort(int arr[], int n) {
    for(int i = 0; i < n; i++) {
        insert(arr, i);
    }
}
